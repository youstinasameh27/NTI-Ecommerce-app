import { ImgUrlPipePipe } from './img-url.pipe-pipe';

describe('ImgUrlPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ImgUrlPipePipe();
    expect(pipe).toBeTruthy();
  });
});
