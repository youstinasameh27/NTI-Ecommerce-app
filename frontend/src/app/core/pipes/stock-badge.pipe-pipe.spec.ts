import { StockBadgePipePipe } from './stock-badge.pipe-pipe';

describe('StockBadgePipePipe', () => {
  it('create an instance', () => {
    const pipe = new StockBadgePipePipe();
    expect(pipe).toBeTruthy();
  });
});
